/**
 * بصير | وحدة التسليم والإغلاق + مراحل المشروع + تقارير الحوادث
 * - قائمة تسليم تفاعلية (مستندات + متطلبات نظامية) قابلة للتحديث والرفع
 * - قائمة ملاحظات (Punch List) مرتبطة بالمقاول مع تتبع الإغلاق
 * - سجل الضمانات مع تنبيهات قرب الانتهاء
 * - سجل تسليم المفاتيح
 * - لوحة فترة ضمان العيوب DLP
 * - تقرير تسليم موحّد قابل للطباعة/التصدير PDF (عربي/إنجليزي)
 * - تكوين مراحل المشروع حسب نوعه (قابل للتخصيص)
 * - تقارير حوادث قابلة للأرشفة والطباعة مرتبطة بتنبيهات الكاميرات
 */
(function () {
  'use strict';

  I18n.registerDict({
    // ---- طباعة/تصدير عام ----
    'اسمح بالنوافذ المنبثقة للطباعة': 'Please allow pop-ups to print',
    'بصير — عيون المالك': 'Bassir — Owner Eyes',
    'تاريخ الإصدار: ': 'Issue Date: ',
    'وثيقة مُصدَّرة آلياً من منصة بصير — Bassir Owner Eyes Platform': 'Document automatically generated by the Bassir Owner Eyes Platform',
    'طباعة / حفظ PDF': 'Print / Save PDF',

    // ---- تبويبات صفحة التسليم ----
    'قائمة التسليم': 'Handover Checklist',
    'قائمة الملاحظات': 'Punch List',
    'الضمانات': 'Warranties',
    'سجل المفاتيح': 'Keys Log',
    'لوحة DLP': 'DLP Dashboard',
    'تقرير التسليم الموحّد': 'Unified Handover Report',

    // ---- قائمة التسليم التفاعلية ----
    'المستندات والتسليمات': 'Documents & Submittals',
    'المتطلبات النظامية والتشغيلية': 'Regulatory & Operational Requirements',
    'قائمة التسليم التفاعلية': 'Interactive Handover Checklist',
    "حدّث حالة كل بند وارفع مستنده داخل المنصة": "Update each item's status and upload its document within the platform",
    'البند': 'Item',
    'المسؤول': 'Responsible',
    'الحالة': 'Status',
    'المستند': 'Document',
    'ملاحظات': 'Notes',
    'مراجعة': 'Review',
    'تحديث': 'Update',
    'تحديث بند التسليم': 'Update Handover Item',
    'لم يبدأ': 'Not Started',
    'قيد الإنجاز': 'In Progress',
    'مكتمل ✓': 'Completed ✓',
    'غير مطلوب': 'Not Required',
    'رفع مستند البند': 'Upload Item Document',
    'حفظ': 'Save',
    'إلغاء': 'Cancel',
    '✅ حُدّث بند التسليم': '✅ Handover item updated',

    // ---- قائمة الملاحظات (Punch List) ----
    'إجمالي الملاحظات': 'Total Punch List Items',
    'مفتوحة': 'Open',
    'مغلقة': 'Closed',
    'نسبة الإغلاق': 'Closure Rate',
    'قائمة الملاحظات مرتبطة بالمقاول المسؤول': 'Punch list items linked to the responsible contractor',
    'تتبع فتح/إغلاق لكل ملاحظة': 'Track open/close status for each item',
    'ملاحظة جديدة': 'New Item',
    'طباعة القائمة': 'Print List',
    'المرجع': 'Reference',
    'الملاحظة': 'Note',
    'الموقع': 'Location',
    'الخطورة': 'Severity',
    'أُثيرت': 'Raised',
    'الحالة / الإغلاق': 'Status / Closure',
    'جوهرية': 'Major',
    'ثانوية': 'Minor',
    'مغلقة ✓': 'Closed ✓',
    'إغلاق': 'Close',
    'لا ملاحظات تسليم': 'No handover items',
    'ملاحظة تسليم جديدة': 'New Handover Punch Item',
    'المقاول المسؤول': 'Responsible Contractor',
    'حفظ وإسناد للمقاول': 'Save & Assign to Contractor',
    'المقاول': 'Contractor',
    'أدخل نص الملاحظة': 'Enter the item text',
    '✅ أُضيفت الملاحظة وأُسندت للمقاول (أُشعر بها)': '✅ Item added and assigned to the contractor (notified)',
    '✅ أُغلقت الملاحظة': '✅ Item closed',
    'الإغلاق': 'Closure',

    // ---- الضمانات ----
    'إجمالي الضمانات': 'Total Warranties',
    'قرب الانتهاء (≤العتبة)': 'Expiring Soon (≤ threshold)',
    'منتهية': 'Expired',
    'سارية': 'Active',
    'تنبيهات قرب انتهاء الضمان:': 'Warranty expiry alerts:',
    'ينتهي خلال': 'expires in',
    'يوم': 'day(s)',
    'سجل ضمانات الموردين والمقاولين من الباطن': 'Supplier & Subcontractor Warranty Register',
    'إضافة ضمان': 'Add Warranty',
    'الكود': 'Code',
    'البند/النظام': 'Item / System',
    'المورّد': 'Supplier',
    'البداية': 'Start Date',
    'الانتهاء': 'End Date',
    'المدة': 'Duration',
    'منتهٍ': 'Expired',
    'ساري': 'Active',
    ' شهر': ' month(s)',
    'لا ضمانات مسجلة': 'No warranties recorded',
    'البند / النظام': 'Item / System',
    'مصاعد، تكييف، عزل...': 'Elevators, HVAC, insulation...',
    'تاريخ البداية': 'Start Date',
    'مدة الضمان (شهر)': 'Warranty Duration (months)',
    'رفع شهادة الضمان': 'Upload Warranty Certificate',
    'أدخل البند': 'Enter the item',
    '✅ أُضيف الضمان': '✅ Warranty added',

    // ---- سجل تسليم المفاتيح ----
    'سجل تسليم المفاتيح': 'Keys Handover Log',
    'تسجيل تسليم': 'Log Handover',
    'المنطقة': 'Area',
    'عدد المفاتيح': 'Number of Keys',
    'سُلّمت إلى': 'Handed To',
    'بواسطة': 'By',
    'التاريخ': 'Date',
    'التوقيع': 'Signature',
    'لم تُسلّم': 'Not handed over',
    'لا سجلات': 'No records',
    'تسجيل تسليم مفاتيح': 'Log Key Handover',
    'التوقيع باسم': 'Signed as',
    'تسجيل': 'Record',
    'أدخل المنطقة': 'Enter the area',
    '✅ سُجّل تسليم المفاتيح': '✅ Key handover recorded',

    // ---- لوحة DLP ----
    'حالة فترة الضمان': 'DLP Status',
    'جارية': 'Ongoing',
    'لم تبدأ': 'Not Started',
    'غير محددة': 'Not Defined',
    'شهر': 'month(s)',
    'المتبقي': 'Remaining',
    'انقضى': 'elapsed',
    'عيوب مفتوحة': 'Open Defects',
    'تحت المسؤولية': 'Under liability',
    'فترة ضمان العيوب (DLP) — التقدم الزمني': 'Defects Liability Period (DLP) — Timeline Progress',
    'تبدأ فترة ضمان العيوب بعد التسليم الابتدائي، وخلالها يلتزم المقاول بمعالجة العيوب الظاهرة على نفقته حتى إصدار شهادة التسليم النهائي.':
      'The Defects Liability Period begins after initial handover; during this period the contractor is obligated to remedy apparent defects at their own expense until the Final Handover Certificate is issued.',
    'العيوب المفتوحة تحت فترة الضمان': 'Open Defects Under the Warranty Period',
    'العيب': 'Defect',
    'أُثير': 'Raised',
    'لا عيوب مفتوحة': 'No open defects',

    // ---- تقرير التسليم الموحّد ----
    'تجميع كامل لحالة التسليم — قابل للتصدير PDF للعميل': 'Complete summary of handover status — exportable as PDF for the client',
    'بنود التسليم المكتملة': 'Completed Handover Items',
    'ملاحظات مفتوحة': 'Open Punch Items',
    'ضمانات مسجلة': 'Registered Warranties',
    'يشمل التقرير: حالة كل بند تسليم ومسؤوله، ملخص قائمة الملاحظات وإغلاقها، سجل الضمانات، وحالة المتطلبات النظامية.':
      'The report includes: the status and owner of each handover item, a summary of punch list items and their closure, the warranty register, and the status of regulatory requirements.',
    'تصدير التقرير (عربي)': 'Export Report (Arabic)',

    // ---- مراحل المشروع ----
    'مراحل المشروع': 'Project Phases',
    'نوع المشروع: ': 'Project Type: ',
    'غير محدد': 'Not Specified',
    'قابلة للتخصيص': 'Customizable',
    'تطبيق قالب حسب النوع...': 'Apply template by type...',
    'قالب افتراضي': 'Default Template',
    'مرحلة': 'Phase',
    'أضف أو احذف المراحل بما يناسب طبيعة المشروع — تنعكس في لوحة القيادة ومقارنات الإنجاز.':
      'Add or remove phases to match the nature of the project — reflected in the dashboard and progress comparisons.',
    'عتبات التنبيه لهذا المشروع': 'Alert Thresholds for this Project',
    'تضبط متى تُطلق التنبيهات وتُحتسب المؤشرات': 'Controls when alerts trigger and how indicators are calculated',
    'حفظ العتبات': 'Save Thresholds',
    'مهلة المراجعة (SLA) — يوم': 'Review Timeframe (SLA) — days',
    'تنبيه قرب انتهاء الضمان — يوم': 'Warranty Expiry Alert — days',
    'حد تأخر المقاول — %': 'Contractor Delay Threshold — %',
    'حد انحراف التقدّم (كهرماني/أحمر) — نقطة': 'Progress Deviation Threshold (amber/red) — points',
    'درجة تنبيه الصحة (عند/دون)': 'Health Alert Grade (at/below)',
    ' — جيد فأدنى': ' — Good or below',
    ' — مقبول فأدنى': ' — Acceptable or below',
    ' — حرج فقط': ' — Critical only',
    ' — أي هبوط': ' — Any decline',
    'تُطبَّق فوراً على تتبع التقديمات (SLA)، وتنبيهات الضمانات، ومقارنة المقاولين، وتنبيه هبوط/تحسّن صحة المشروع.':
      'Applied immediately to submittal tracking (SLA), warranty alerts, contractor comparisons, and project health decline/improvement alerts.',
    'المرحلة': 'Phase',
    'مخطط': 'Planned',
    'فعلي': 'Actual',
    'الإنجاز': 'Progress',
    'لا مراحل — طبّق قالباً أو أضف مرحلة': 'No phases — apply a template or add a phase',
    '✅ حُفظت عتبات التنبيه — طُبّقت على مؤشرات هذا المشروع': "✅ Alert thresholds saved — applied to this project's indicators",
    'تطبيق قالب "': 'Apply template "',
    'افتراضي': 'Default',
    '"؟ سيضيف المراحل غير الموجودة.': '"؟ This will add any missing phases.',
    '✅ طُبّق القالب وأُضيفت المراحل': '✅ Template applied and phases added',
    'حذف هذه المرحلة؟': 'Delete this phase?',
    'حذف': 'Delete',
    'حُذفت المرحلة': 'Phase deleted',

    // ---- نافذة مرحلة ----
    'تحديث مرحلة': 'Update Phase',
    'مرحلة جديدة': 'New Phase',
    'من المكتبة (اختياري)': 'From Library (optional)',
    '— مخصصة —': '— Custom —',
    'اسم المرحلة': 'Phase Name',
    'بداية مخططة': 'Planned Start',
    'نهاية مخططة': 'Planned End',
    'بداية فعلية': 'Actual Start',
    'نهاية فعلية': 'Actual End',
    'نسبة الإنجاز %': 'Progress %',
    'أدخل اسم المرحلة': 'Enter the phase name',
    '✅ حُفظت المرحلة': '✅ Phase saved',

    // ---- محفظة المشاريع ----
    'عدد المشاريع': 'Number of Projects',
    'تحت الإشراف': 'Under supervision',
    'متوسط صحة المحفظة': 'Average Portfolio Health',
    'من 100': 'out of 100',
    'مشاريع تحت المخاطرة': 'Projects At Risk',
    'فئة C أو D': 'Grade C or D',
    'إجمالي الميزانيات': 'Total Budgets',
    'الإنجاز المرجّح ': 'Weighted Progress ',
    'محفظة المشاريع — مقارنة الصحة': 'Project Portfolio — Health Comparison',
    'اضغط "فتح" للعمل على أي مشروع': 'Click "Open" to work on any project',
    'المشروع': 'Project',
    'النوع': 'Type',
    'الإنجاز (فعلي/مخطط)': 'Progress (Actual/Planned)',
    'التكلفة': 'Cost',
    'مؤشر الصحة': 'Health Score',
    'الاتجاه': 'Trend',
    'مخاطر مفتوحة': 'Open Risks',
    'مخطط ': 'Planned ',
    'من ': 'of ',
    'الحالي': 'Current',
    'فتح ←': 'Open ←',
    'تصدير تقرير المحفظة PDF': 'Export Portfolio Report PDF',
    'تقرير محفظة المشاريع': 'Project Portfolio Report',
    'عدد المشاريع: ': 'Number of Projects: ',
    '· متوسط الصحة: ': '· Average Health: ',
    '· تحت المخاطرة: ': '· At Risk: ',
    '· إجمالي الميزانيات: ': '· Total Budgets: ',
    '· الإنجاز المرجّح: ': '· Weighted Progress: ',
    'الإنجاز الفعلي': 'Actual Progress',
    'المخطط': 'Planned',
    'الفئة': 'Grade'
  });

  const VS = window.ViewsShared;
  const esc = VS.esc, pill = VS.pill, toast = VS.toast, modal = VS.modal;

  function contractorName(ctx, id) {
    const c = (ctx.Sall || ctx.S).contractors.find(function (x) { return x.id === id; });
    return c ? c.name : id || '—';
  }
  function todayStr() { return new Date().toISOString().slice(0, 10); }
  function daysBetween(a, b) { return Math.round((new Date(b) - new Date(a)) / 86400000); }
  function daysFromNow(dateStr) { if (!dateStr) return null; return Math.round((new Date(dateStr) - Date.now()) / 86400000); }

  const HND_STATUS = { pending: ['لم يبدأ', 'p-muted'], in_progress: ['قيد الإنجاز', 'p-warn'], done: ['مكتمل ✓', 'p-ok'], na: ['غير مطلوب', 'p-muted'] };
  function hndPill(s) { const v = HND_STATUS[s] || [s, 'p-muted']; return '<span class="pill ' + v[1] + '">' + esc(I18n.t(v[0])) + '</span>'; }

  // ============ الطباعة/التصدير PDF (نافذة طباعة منسّقة) ============
  function printDoc(title, bodyHtml) {
    const w = window.open('', '_blank');
    if (!w) { toast(I18n.t('اسمح بالنوافذ المنبثقة للطباعة'), true); return; }
    w.document.write(
      '<!DOCTYPE html><html lang="ar" dir="rtl"><head><meta charset="utf-8"><title>' + esc(title) + '</title>' +
      '<style>body{font-family:Tahoma,Arial,sans-serif;color:#111;margin:32px;line-height:1.8}' +
      'h1{font-size:22px;border-bottom:3px solid #0b8457;padding-bottom:8px;color:#0b5}' +
      'h2{font-size:16px;margin-top:22px;color:#0b8457;border-right:4px solid #0b8457;padding-right:10px}' +
      'table{width:100%;border-collapse:collapse;margin:10px 0;font-size:12.5px}' +
      'th,td{border:1px solid #bbb;padding:7px 9px;text-align:right}th{background:#eef6f2}' +
      '.muted{color:#666;font-size:12px}.ok{color:#0b8457;font-weight:700}.bad{color:#c0392b;font-weight:700}' +
      '.head{display:flex;justify-content:space-between;align-items:center}.logo{font-size:26px}' +
      '.badge{display:inline-block;border:1px solid #999;border-radius:20px;padding:2px 10px;font-size:11px}' +
      '@media print{.noprint{display:none}}</style></head><body>' +
      '<div class="head"><div><span class="logo">👁 ' + I18n.t('بصير — عيون المالك') + '</span></div>' +
      '<div class="muted">' + I18n.t('تاريخ الإصدار: ') + todayStr() + '</div></div>' +
      bodyHtml +
      '<p class="muted" style="margin-top:26px;border-top:1px solid #ccc;padding-top:10px">' + I18n.t('وثيقة مُصدَّرة آلياً من منصة بصير — Bassir Owner Eyes Platform') + '</p>' +
      '<div class="noprint" style="margin-top:20px"><button onclick="window.print()" style="padding:10px 22px;font-size:14px;cursor:pointer;background:#0b8457;color:#fff;border:0;border-radius:8px">🖨 ' + I18n.t('طباعة / حفظ PDF') + '</button></div>' +
      '</body></html>');
    w.document.close();
  }

  // ============ صفحة التسليم والإغلاق (Handover) ============
  const hoState = { tab: 'checklist' };

  function renderHandover(el, ctx) {
    const tabs = [
      ['checklist', '📋 ' + I18n.t('قائمة التسليم')],
      ['punch', '📌 ' + I18n.t('قائمة الملاحظات')],
      ['warranties', '🛡️ ' + I18n.t('الضمانات')],
      ['keys', '🔑 ' + I18n.t('سجل المفاتيح')],
      ['dlp', '🏁 ' + I18n.t('لوحة DLP')],
      ['report', '📄 ' + I18n.t('تقرير التسليم الموحّد')]
    ];
    el.innerHTML =
      '<div class="tabs">' + tabs.map(function (t) {
        return '<div class="tab ' + (hoState.tab === t[0] ? 'active' : '') + '" data-hotab="' + t[0] + '">' + t[1] + '</div>';
      }).join('') + '</div><div id="ho-body"></div>';
    el.querySelectorAll('[data-hotab]').forEach(function (t) {
      t.addEventListener('click', function () { hoState.tab = t.getAttribute('data-hotab'); renderHandover(el, ctx); });
    });
    const body = el.querySelector('#ho-body');
    if (hoState.tab === 'checklist') renderChecklist(body, ctx);
    else if (hoState.tab === 'punch') renderPunch(body, ctx);
    else if (hoState.tab === 'warranties') renderWarranties(body, ctx);
    else if (hoState.tab === 'keys') renderKeys(body, ctx);
    else if (hoState.tab === 'dlp') renderDlp(body, ctx);
    else renderUnifiedReport(body, ctx);
  }

  // ---- قائمة التسليم التفاعلية (مستندات + متطلبات نظامية) ----
  function renderChecklist(el, ctx) {
    const items = ctx.S.handoverItems || [];
    const canEdit = ['consultant', 'admin', 'owner_rep'].indexOf(ctx.U.role) !== -1;
    const groups = [
      ['docs', '📁 ' + I18n.t('المستندات والتسليمات')],
      ['regulatory', '🏛️ ' + I18n.t('المتطلبات النظامية والتشغيلية')]
    ];
    const done = items.filter(function (i) { return i.status === 'done'; }).length;
    const pct = items.length ? Math.round(done / items.length * 100) : 0;

    el.innerHTML =
      '<div class="card mb"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
      '<h3 style="margin:0">✅ ' + I18n.t('قائمة التسليم التفاعلية') + ' <span class="hint">' + I18n.t('حدّث حالة كل بند وارفع مستنده داخل المنصة') + '</span></h3>' +
      '<div class="flex" style="gap:8px;align-items:center"><div class="bar" style="width:160px"><i style="width:' + pct + '%"></i></div>' +
      '<b class="num">' + done + '/' + items.length + ' (' + pct + '%)</b></div></div></div>' +
      groups.map(function (g) {
        const gi = items.filter(function (i) { return i.group === g[0]; });
        return '<div class="card mb"><h3>' + g[1] + '</h3>' +
          '<div class="tbl-wrap"><table class="tbl"><thead><tr><th>' + I18n.t('البند') + '</th><th>' + I18n.t('المسؤول') + '</th><th>' + I18n.t('الحالة') + '</th><th>' + I18n.t('المستند') + '</th><th>' + I18n.t('ملاحظات') + '</th>' + (canEdit ? '<th></th>' : '') + '</tr></thead><tbody>' +
          gi.map(function (it) {
            return '<tr><td><b>' + esc(it.title) + '</b><div class="small muted" dir="ltr" style="text-align:right">' + esc(it.en || '') + '</div></td>' +
              '<td class="small">' + esc(it.responsible || '—') + '</td>' +
              '<td>' + hndPill(it.status) + '</td>' +
              '<td class="small">' + (it.file
                ? '📎 ' + VS.att(it.file) + ' <button class="btn ghost sm" data-hreview="' + it.id + '">🔍 ' + I18n.t('مراجعة') + '</button>'
                : '<span class="muted">—</span>') + '</td>' +
              '<td class="small" style="max-width:220px">' + esc(it.notes || '') + (it.date ? '<div class="muted num">' + esc(it.date) + '</div>' : '') + '</td>' +
              (canEdit ? '<td><button class="btn ghost sm" data-hedit="' + it.id + '">' + I18n.t('تحديث') + '</button></td>' : '') +
              '</tr>';
          }).join('') + '</tbody></table></div></div>';
      }).join('');

    // مراجعة مستند التسليم المرفوع داخل المنصة (عارض PDF/صور مع الترميز)
    el.querySelectorAll('[data-hreview]').forEach(function (b) {
      b.addEventListener('click', function () {
        const it = items.find(function (x) { return x.id === b.getAttribute('data-hreview'); });
        if (it && window.DrawingViewer) window.DrawingViewer.open(ctx, 'handoverItems', it, { canEdit: canEdit, canReview: false });
      });
    });

    el.querySelectorAll('[data-hedit]').forEach(function (b) {
      b.addEventListener('click', function () {
        const it = items.find(function (x) { return x.id === b.getAttribute('data-hedit'); });
        const m = modal(
          '<h3>' + I18n.t('تحديث بند التسليم') + '</h3><div class="m-sub">' + esc(it.title) + '</div>' +
          '<label class="fl">' + I18n.t('الحالة') + '</label><select class="inp" id="he-status">' +
          Object.keys(HND_STATUS).map(function (k) { return '<option value="' + k + '"' + (it.status === k ? ' selected' : '') + '>' + I18n.t(HND_STATUS[k][0]) + '</option>'; }).join('') + '</select>' +
          '<label class="fl">' + I18n.t('المسؤول') + '</label><input class="inp" id="he-resp" value="' + esc(it.responsible || '') + '">' +
          '<label class="fl">' + I18n.t('ملاحظات') + '</label><textarea class="inp" id="he-notes" rows="2">' + esc(it.notes || '') + '</textarea>' +
          '<label class="fl">' + I18n.t('رفع مستند البند') + '</label><input class="inp" id="he-file" type="file">' +
          '<div class="m-actions"><button class="btn" id="he-ok">' + I18n.t('حفظ') + '</button><button class="btn mutedb" id="he-cancel">' + I18n.t('إلغاء') + '</button></div>'
        );
        m.querySelector('#he-cancel').addEventListener('click', function () { m.remove(); });
        m.querySelector('#he-ok').addEventListener('click', async function () {
          try {
            const patch = {
              status: m.querySelector('#he-status').value,
              responsible: m.querySelector('#he-resp').value,
              notes: m.querySelector('#he-notes').value,
              date: todayStr()
            };
            const f = m.querySelector('#he-file').files[0];
            if (f) patch.file = await Api.upload(f, { category: 'وثائق فنية' });
            await Api.update('handoverItems', it.id, patch);
            m.remove(); toast(I18n.t('✅ حُدّث بند التسليم')); ctx.refresh();
          } catch (e) { toast(e.message, true); }
        });
      });
    });
  }

  // ---- قائمة الملاحظات (Punch List) ----
  function renderPunch(el, ctx) {
    const items = (ctx.S.punchList || []).slice().sort(function (a, b) { return (a.status === 'open' ? 0 : 1) - (b.status === 'open' ? 0 : 1); });
    const canManage = ['consultant', 'admin'].indexOf(ctx.U.role) !== -1;
    const isContractor = ctx.U.role === 'contractor';
    const open = items.filter(function (x) { return x.status === 'open'; }).length;
    const closed = items.length - open;
    // تجميع حسب المقاول
    const byC = {};
    items.forEach(function (it) { (byC[it.contractorId] = byC[it.contractorId] || []).push(it); });

    el.innerHTML =
      '<div class="grid g4 mb">' +
      '<div class="card kpi"><div class="lbl">' + I18n.t('إجمالي الملاحظات') + '</div><div class="val num">' + items.length + '</div></div>' +
      '<div class="card kpi ' + (open ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('مفتوحة') + '</div><div class="val num">' + open + '</div></div>' +
      '<div class="card kpi k-ok"><div class="lbl">' + I18n.t('مغلقة') + '</div><div class="val num">' + closed + '</div></div>' +
      '<div class="card kpi k-info"><div class="lbl">' + I18n.t('نسبة الإغلاق') + '</div><div class="val num">' + (items.length ? Math.round(closed / items.length * 100) : 0) + '%</div></div>' +
      '</div>' +
      '<div class="card"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
      '<h3 style="margin:0">📌 ' + I18n.t('قائمة الملاحظات مرتبطة بالمقاول المسؤول') + ' <span class="hint">' + I18n.t('تتبع فتح/إغلاق لكل ملاحظة') + '</span></h3>' +
      '<div class="flex">' + (canManage ? '<button class="btn sm" id="pl-add">➕ ' + I18n.t('ملاحظة جديدة') + '</button>' : '') +
      '<button class="btn ghost sm" id="pl-print">🖨 ' + I18n.t('طباعة القائمة') + '</button></div></div>' +
      Object.keys(byC).map(function (cid) {
        const list = byC[cid];
        const co = list.filter(function (x) { return x.status === 'open'; }).length;
        return '<div style="margin-top:14px"><b>👷 ' + esc(contractorName(ctx, cid)) + '</b> ' +
          '<span class="pill ' + (co ? 'p-warn' : 'p-ok') + '">' + co + ' ' + I18n.t('مفتوحة') + ' / ' + list.length + '</span>' +
          '<div class="tbl-wrap" style="margin-top:8px"><table class="tbl"><thead><tr><th>' + I18n.t('المرجع') + '</th><th>' + I18n.t('الملاحظة') + '</th><th>' + I18n.t('الموقع') + '</th><th>' + I18n.t('الخطورة') + '</th><th>' + I18n.t('أُثيرت') + '</th><th>' + I18n.t('الحالة / الإغلاق') + '</th><th></th></tr></thead><tbody>' +
          list.map(function (it) {
            return '<tr><td class="num small"><b>' + esc(it.ref || '') + '</b>' + (it.docCode ? '<div class="muted" style="font-size:10px;color:var(--accent2)">' + esc(it.docCode) + '</div>' : '') + '</td>' +
              '<td>' + esc(it.title) + (it.notes ? '<div class="small muted">' + esc(it.notes) + '</div>' : '') + '</td>' +
              '<td class="small">' + esc(VS.floorName(ctx, it.location) || it.location || '') + '</td>' +
              '<td>' + (it.severity === 'major' ? '<span class="pill p-danger">' + I18n.t('جوهرية') + '</span>' : '<span class="pill p-warn">' + I18n.t('ثانوية') + '</span>') + '</td>' +
              '<td class="small muted num">' + esc(it.raisedDate || '') + '</td>' +
              '<td>' + (it.status === 'closed' ? '<span class="pill p-ok">' + I18n.t('مغلقة ✓') + '</span><div class="small muted num">' + esc(it.closedDate || '') + '</div>' : '<span class="pill p-warn">' + I18n.t('مفتوحة') + '</span>') + '</td>' +
              '<td>' + ((canManage || isContractor) && it.status === 'open' ? '<button class="btn sm" data-plclose="' + it.id + '">✅ ' + I18n.t('إغلاق') + '</button>' : '') + '</td></tr>';
          }).join('') + '</tbody></table></div></div>';
      }).join('') +
      (items.length ? '' : '<div class="empty"><div class="e-ico">📌</div>' + I18n.t('لا ملاحظات تسليم') + '</div>') +
      '</div>';

    const addBtn = el.querySelector('#pl-add');
    if (addBtn) addBtn.addEventListener('click', function () {
      const m = modal(
        '<h3>➕ ' + I18n.t('ملاحظة تسليم جديدة') + '</h3>' +
        '<label class="fl">' + I18n.t('المقاول المسؤول') + '</label><select class="inp" id="pl-cont">' +
        ctx.S.contractors.map(function (c) { return '<option value="' + c.id + '">' + esc(c.name) + '</option>'; }).join('') + '</select>' +
        '<label class="fl">' + I18n.t('المرجع') + '</label><input class="inp num" id="pl-ref" placeholder="PL-001">' +
        '<label class="fl">' + I18n.t('الملاحظة') + '</label><input class="inp" id="pl-title">' +
        '<div class="grid g2"><div><label class="fl">' + I18n.t('الموقع') + '</label><select class="inp" id="pl-loc">' +
        ctx.S.projects[0].floors.map(function (f) { return '<option value="' + f.id + '">' + esc(f.name) + '</option>'; }).join('') + '</select></div>' +
        '<div><label class="fl">' + I18n.t('الخطورة') + '</label><select class="inp" id="pl-sev"><option value="minor">' + I18n.t('ثانوية') + '</option><option value="major">' + I18n.t('جوهرية') + '</option></select></div></div>' +
        '<div class="m-actions"><button class="btn" id="pl-ok">' + I18n.t('حفظ وإسناد للمقاول') + '</button><button class="btn mutedb" id="pl-cancel">' + I18n.t('إلغاء') + '</button></div>'
      );
      m.querySelector('#pl-cancel').addEventListener('click', function () { m.remove(); });
      m.querySelector('#pl-ok').addEventListener('click', async function () {
        const title = m.querySelector('#pl-title').value.trim();
        if (!title) { toast(I18n.t('أدخل نص الملاحظة'), true); return; }
        try {
          await Api.create('punchList', {
            contractorId: m.querySelector('#pl-cont').value,
            ref: m.querySelector('#pl-ref').value || 'PL-' + Math.floor(Math.random() * 900 + 100),
            title: title, location: m.querySelector('#pl-loc').value, severity: m.querySelector('#pl-sev').value,
            status: 'open', raisedDate: todayStr(), closedDate: null
          });
          m.remove(); toast(I18n.t('✅ أُضيفت الملاحظة وأُسندت للمقاول (أُشعر بها)')); ctx.refresh();
        } catch (e) { toast(e.message, true); }
      });
    });

    el.querySelectorAll('[data-plclose]').forEach(function (b) {
      b.addEventListener('click', async function () {
        try {
          await Api.update('punchList', b.getAttribute('data-plclose'), { status: 'closed', closedDate: todayStr() });
          toast(I18n.t('✅ أُغلقت الملاحظة')); ctx.refresh();
        } catch (e) { toast(e.message, true); }
      });
    });

    el.querySelector('#pl-print').addEventListener('click', function () {
      printDoc(I18n.t('قائمة الملاحظات') + ' — Punch List', punchReportHtml(ctx, items));
    });
  }

  function punchReportHtml(ctx, items) {
    return '<h1>' + I18n.t('قائمة الملاحظات') + ' — Punch List</h1>' +
      '<p class="muted">' + I18n.t('المشروع') + ': ' + esc(ctx.S.projects[0].name) + '</p>' +
      '<table><thead><tr><th>' + I18n.t('المرجع') + '</th><th>' + I18n.t('المقاول') + '</th><th>' + I18n.t('الملاحظة') + '</th><th>' + I18n.t('الموقع') + '</th><th>' + I18n.t('الخطورة') + '</th><th>' + I18n.t('الحالة') + '</th><th>' + I18n.t('الإغلاق') + '</th></tr></thead><tbody>' +
      items.map(function (it) {
        return '<tr><td>' + esc(it.ref || '') + '</td><td>' + esc(contractorName(ctx, it.contractorId)) + '</td><td>' + esc(it.title) + '</td>' +
          '<td>' + esc(VS.floorName(ctx, it.location) || it.location || '') + '</td><td>' + (it.severity === 'major' ? I18n.t('جوهرية') : I18n.t('ثانوية')) + '</td>' +
          '<td class="' + (it.status === 'closed' ? 'ok' : 'bad') + '">' + (it.status === 'closed' ? I18n.t('مغلقة') : I18n.t('مفتوحة')) + '</td><td>' + esc(it.closedDate || '—') + '</td></tr>';
      }).join('') + '</tbody></table>';
  }

  // ---- الضمانات + تنبيهات قرب الانتهاء ----
  function renderWarranties(el, ctx) {
    const items = (ctx.S.warranties || []).slice().sort(function (a, b) { return String(a.endDate).localeCompare(String(b.endDate)); });
    const canAdd = ['contractor', 'consultant', 'admin'].indexOf(ctx.U.role) !== -1;
    const warnDays = VS.thresholds(ctx.S.projects[0]).warrantyWarnDays;
    const expiringSoon = items.filter(function (w) { const d = daysFromNow(w.endDate); return d != null && d >= 0 && d <= warnDays; });
    const expired = items.filter(function (w) { const d = daysFromNow(w.endDate); return d != null && d < 0; });

    el.innerHTML =
      '<div class="grid g4 mb">' +
      '<div class="card kpi"><div class="lbl">' + I18n.t('إجمالي الضمانات') + '</div><div class="val num">' + items.length + '</div></div>' +
      '<div class="card kpi ' + (expiringSoon.length ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('قرب الانتهاء (≤العتبة)') + '</div><div class="val num">' + expiringSoon.length + '</div></div>' +
      '<div class="card kpi ' + (expired.length ? 'k-danger' : 'k-ok') + '"><div class="lbl">' + I18n.t('منتهية') + '</div><div class="val num">' + expired.length + '</div></div>' +
      '<div class="card kpi k-info"><div class="lbl">' + I18n.t('سارية') + '</div><div class="val num">' + (items.length - expired.length) + '</div></div>' +
      '</div>' +
      (expiringSoon.length ? '<div class="card mb" style="border-color:var(--warn)"><b class="small" style="color:var(--warn)">⏰ ' + I18n.t('تنبيهات قرب انتهاء الضمان:') + '</b>' +
        expiringSoon.map(function (w) { return '<div class="small" style="margin-top:6px">• ' + esc(w.item) + ' — ' + I18n.t('ينتهي خلال') + ' <b class="num">' + daysFromNow(w.endDate) + '</b> ' + I18n.t('يوم') + ' (' + esc(w.endDate) + ')</div>'; }).join('') + '</div>' : '') +
      '<div class="card"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
      '<h3 style="margin:0">🛡️ ' + I18n.t('سجل ضمانات الموردين والمقاولين من الباطن') + '</h3>' +
      (canAdd ? '<button class="btn sm" id="wr-add">➕ ' + I18n.t('إضافة ضمان') + '</button>' : '') + '</div>' +
      '<div class="tbl-wrap"><table class="tbl"><thead><tr><th>' + I18n.t('الكود') + '</th><th>' + I18n.t('البند/النظام') + '</th><th>' + I18n.t('المورّد') + '</th><th>' + I18n.t('المقاول') + '</th><th>' + I18n.t('البداية') + '</th><th>' + I18n.t('الانتهاء') + '</th><th>' + I18n.t('المدة') + '</th><th>' + I18n.t('الحالة') + '</th></tr></thead><tbody>' +
      items.map(function (w) {
        const d = daysFromNow(w.endDate);
        const st = d == null ? '' : d < 0 ? '<span class="pill p-danger">' + I18n.t('منتهٍ') + '</span>' : d <= 90 ? '<span class="pill p-warn">≤ ' + d + ' ' + I18n.t('يوم') + '</span>' : '<span class="pill p-ok">' + I18n.t('ساري') + '</span>';
        return '<tr><td class="num small"><b style="color:var(--accent2)">' + esc(w.docCode || '') + '</b></td>' +
          '<td>' + esc(w.item) + '</td><td class="small">' + esc(w.supplier || '') + '</td>' +
          '<td class="small">' + esc(contractorName(ctx, w.contractorId)) + '</td>' +
          '<td class="small muted num">' + esc(w.startDate || '') + '</td><td class="small num">' + esc(w.endDate || '') + '</td>' +
          '<td class="num small">' + (w.months || '—') + I18n.t(' شهر') + '</td><td>' + st + '</td></tr>';
      }).join('') + '</tbody></table></div>' +
      (items.length ? '' : '<div class="empty"><div class="e-ico">🛡️</div>' + I18n.t('لا ضمانات مسجلة') + '</div>') + '</div>';

    const addBtn = el.querySelector('#wr-add');
    if (addBtn) addBtn.addEventListener('click', function () {
      const m = modal(
        '<h3>➕ ' + I18n.t('إضافة ضمان') + '</h3>' +
        '<label class="fl">' + I18n.t('البند / النظام') + '</label><input class="inp" id="w-item" placeholder="' + I18n.t('مصاعد، تكييف، عزل...') + '">' +
        '<label class="fl">' + I18n.t('المورّد') + '</label><input class="inp" id="w-sup">' +
        '<label class="fl">' + I18n.t('المقاول') + '</label><select class="inp" id="w-cont">' +
        ctx.S.contractors.map(function (c) { return '<option value="' + c.id + '">' + esc(c.name) + '</option>'; }).join('') + '</select>' +
        '<div class="grid g2"><div><label class="fl">' + I18n.t('تاريخ البداية') + '</label><input class="inp" id="w-start" type="date" value="' + todayStr() + '"></div>' +
        '<div><label class="fl">' + I18n.t('مدة الضمان (شهر)') + '</label><input class="inp num" id="w-months" type="number" value="12"></div></div>' +
        '<label class="fl">' + I18n.t('رفع شهادة الضمان') + '</label><input class="inp" id="w-file" type="file">' +
        '<div class="m-actions"><button class="btn" id="w-ok">' + I18n.t('حفظ') + '</button><button class="btn mutedb" id="w-cancel">' + I18n.t('إلغاء') + '</button></div>'
      );
      m.querySelector('#w-cancel').addEventListener('click', function () { m.remove(); });
      m.querySelector('#w-ok').addEventListener('click', async function () {
        const item = m.querySelector('#w-item').value.trim();
        if (!item) { toast(I18n.t('أدخل البند'), true); return; }
        const start = m.querySelector('#w-start').value || todayStr();
        const months = Number(m.querySelector('#w-months').value) || 12;
        const end = new Date(start); end.setMonth(end.getMonth() + months);
        try {
          const data = {
            item: item, supplier: m.querySelector('#w-sup').value, contractorId: m.querySelector('#w-cont').value,
            startDate: start, endDate: end.toISOString().slice(0, 10), months: months
          };
          const f = m.querySelector('#w-file').files[0];
          if (f) data.file = await Api.upload(f, { category: 'وثائق فنية' });
          await Api.create('warranties', data);
          m.remove(); toast(I18n.t('✅ أُضيف الضمان')); ctx.refresh();
        } catch (e) { toast(e.message, true); }
      });
    });
  }

  // ---- سجل تسليم المفاتيح ----
  function renderKeys(el, ctx) {
    const items = ctx.S.keysLog || [];
    const canAdd = ['consultant', 'admin', 'owner_rep'].indexOf(ctx.U.role) !== -1;
    el.innerHTML =
      '<div class="card"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
      '<h3 style="margin:0">🔑 ' + I18n.t('سجل تسليم المفاتيح') + '</h3>' + (canAdd ? '<button class="btn sm" id="k-add">➕ ' + I18n.t('تسجيل تسليم') + '</button>' : '') + '</div>' +
      '<div class="tbl-wrap"><table class="tbl"><thead><tr><th>' + I18n.t('المنطقة') + '</th><th>' + I18n.t('عدد المفاتيح') + '</th><th>' + I18n.t('سُلّمت إلى') + '</th><th>' + I18n.t('بواسطة') + '</th><th>' + I18n.t('التاريخ') + '</th><th>' + I18n.t('التوقيع') + '</th></tr></thead><tbody>' +
      items.map(function (k) {
        return '<tr><td>' + esc(k.area) + '</td><td class="num">' + (k.count || '') + '</td><td class="small">' + esc(k.handedTo || '') + '</td>' +
          '<td class="small">' + esc(k.by || '') + '</td><td class="small muted num">' + (k.date || '<span class="pill p-warn">' + I18n.t('لم تُسلّم') + '</span>') + '</td>' +
          '<td class="small">' + (k.signature ? '✍️ ' + esc(k.signature) : '—') + '</td></tr>';
      }).join('') + '</tbody></table></div>' +
      (items.length ? '' : '<div class="empty"><div class="e-ico">🔑</div>' + I18n.t('لا سجلات') + '</div>') + '</div>';

    const addBtn = el.querySelector('#k-add');
    if (addBtn) addBtn.addEventListener('click', function () {
      const m = modal(
        '<h3>🔑 ' + I18n.t('تسجيل تسليم مفاتيح') + '</h3>' +
        '<label class="fl">' + I18n.t('المنطقة') + '</label><input class="inp" id="k-area">' +
        '<div class="grid g2"><div><label class="fl">' + I18n.t('عدد المفاتيح') + '</label><input class="inp num" id="k-count" type="number"></div>' +
        '<div><label class="fl">' + I18n.t('سُلّمت إلى') + '</label><input class="inp" id="k-to"></div></div>' +
        '<label class="fl">' + I18n.t('التوقيع باسم') + '</label><input class="inp" id="k-sig" value="' + esc(ctx.U.name) + '">' +
        '<div class="m-actions"><button class="btn" id="k-ok">' + I18n.t('تسجيل') + '</button><button class="btn mutedb" id="k-cancel">' + I18n.t('إلغاء') + '</button></div>'
      );
      m.querySelector('#k-cancel').addEventListener('click', function () { m.remove(); });
      m.querySelector('#k-ok').addEventListener('click', async function () {
        const area = m.querySelector('#k-area').value.trim();
        if (!area) { toast(I18n.t('أدخل المنطقة'), true); return; }
        try {
          await Api.create('keysLog', {
            area: area, count: Number(m.querySelector('#k-count').value) || 0,
            handedTo: m.querySelector('#k-to').value, by: ctx.U.name, date: todayStr(),
            signature: m.querySelector('#k-sig').value
          });
          m.remove(); toast(I18n.t('✅ سُجّل تسليم المفاتيح')); ctx.refresh();
        } catch (e) { toast(e.message, true); }
      });
    });
  }

  // ---- لوحة فترة ضمان العيوب DLP ----
  function renderDlp(el, ctx) {
    const P = ctx.S.projects[0] || {};
    const start = P.dlpStart, months = P.dlpMonths || 12;
    let end = '';
    if (start) { const e = new Date(start); e.setMonth(e.getMonth() + months); end = e.toISOString().slice(0, 10); }
    const total = start && end ? daysBetween(start, end) : 0;
    const elapsed = start ? Math.max(0, Math.min(total, daysBetween(start, todayStr()))) : 0;
    const remaining = Math.max(0, total - elapsed);
    const started = start && new Date(start) <= new Date();
    const pct = total ? Math.round(elapsed / total * 100) : 0;
    // عيوب فترة الضمان = ملاحظات مفتوحة تُعامل كعيوب + عناصر handoverDocs من نوع dlp
    const defects = (ctx.S.punchList || []).filter(function (x) { return x.status === 'open'; });

    el.innerHTML =
      '<div class="grid g4 mb">' +
      '<div class="card kpi ' + (started ? 'k-info' : '') + '"><div class="lbl">' + I18n.t('حالة فترة الضمان') + '</div><div class="val" style="font-size:18px">' + (started ? I18n.t('جارية') : (start ? I18n.t('لم تبدأ') : I18n.t('غير محددة'))) + '</div><div class="sub num">' + (start ? esc(start) + ' ← ' + esc(end) : '—') + '</div></div>' +
      '<div class="card kpi"><div class="lbl">' + I18n.t('المدة') + '</div><div class="val num">' + months + '</div><div class="sub">' + I18n.t('شهر') + '</div></div>' +
      '<div class="card kpi ' + (remaining < 60 && started ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('المتبقي') + '</div><div class="val num">' + remaining + '</div><div class="sub">' + I18n.t('يوم') + (started ? ' (' + pct + '% ' + I18n.t('انقضى') + ')' : '') + '</div></div>' +
      '<div class="card kpi ' + (defects.length ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('عيوب مفتوحة') + '</div><div class="val num">' + defects.length + '</div><div class="sub">' + I18n.t('تحت المسؤولية') + '</div></div>' +
      '</div>' +
      '<div class="card mb"><h3>🏁 ' + I18n.t('فترة ضمان العيوب (DLP) — التقدم الزمني') + '</h3>' +
      '<div class="flex" style="gap:10px;align-items:center"><div class="bar" style="flex:1;height:16px"><i style="width:' + pct + '%;background:var(--accent2)"></i></div><b class="num">' + pct + '%</b></div>' +
      '<div class="small muted mt">' + I18n.t('تبدأ فترة ضمان العيوب بعد التسليم الابتدائي، وخلالها يلتزم المقاول بمعالجة العيوب الظاهرة على نفقته حتى إصدار شهادة التسليم النهائي.') + '</div></div>' +
      '<div class="card"><h3>🔧 ' + I18n.t('العيوب المفتوحة تحت فترة الضمان') + '</h3>' +
      (defects.length ?
        '<div class="tbl-wrap"><table class="tbl"><thead><tr><th>' + I18n.t('المرجع') + '</th><th>' + I18n.t('العيب') + '</th><th>' + I18n.t('المقاول المسؤول') + '</th><th>' + I18n.t('الموقع') + '</th><th>' + I18n.t('الخطورة') + '</th><th>' + I18n.t('أُثير') + '</th></tr></thead><tbody>' +
        defects.map(function (it) {
          return '<tr><td class="num small">' + esc(it.ref || '') + '</td><td>' + esc(it.title) + '</td><td class="small">' + esc(contractorName(ctx, it.contractorId)) + '</td>' +
            '<td class="small">' + esc(VS.floorName(ctx, it.location) || it.location || '') + '</td>' +
            '<td>' + (it.severity === 'major' ? '<span class="pill p-danger">' + I18n.t('جوهرية') + '</span>' : '<span class="pill p-warn">' + I18n.t('ثانوية') + '</span>') + '</td>' +
            '<td class="small muted num">' + esc(it.raisedDate || '') + '</td></tr>';
        }).join('') + '</tbody></table></div>'
        : '<div class="empty"><div class="e-ico">✅</div>' + I18n.t('لا عيوب مفتوحة') + '</div>') + '</div>';
  }

  // ---- تقرير التسليم الموحّد (طباعة/PDF عربي/إنجليزي) ----
  function renderUnifiedReport(el, ctx) {
    const items = ctx.S.handoverItems || [];
    const punch = ctx.S.punchList || [];
    const warr = ctx.S.warranties || [];
    const done = items.filter(function (i) { return i.status === 'done'; }).length;
    const openPunch = punch.filter(function (x) { return x.status === 'open'; }).length;

    el.innerHTML =
      '<div class="card"><h3>📄 ' + I18n.t('تقرير التسليم الموحّد') + ' <span class="hint">' + I18n.t('تجميع كامل لحالة التسليم — قابل للتصدير PDF للعميل') + '</span></h3>' +
      '<div class="grid g3 mb" style="margin-top:14px">' +
      '<div class="card kpi"><div class="lbl">' + I18n.t('بنود التسليم المكتملة') + '</div><div class="val num">' + done + '/' + items.length + '</div></div>' +
      '<div class="card kpi ' + (openPunch ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('ملاحظات مفتوحة') + '</div><div class="val num">' + openPunch + '</div></div>' +
      '<div class="card kpi k-info"><div class="lbl">' + I18n.t('ضمانات مسجلة') + '</div><div class="val num">' + warr.length + '</div></div>' +
      '</div>' +
      '<div class="small muted mb">' + I18n.t('يشمل التقرير: حالة كل بند تسليم ومسؤوله، ملخص قائمة الملاحظات وإغلاقها، سجل الضمانات، وحالة المتطلبات النظامية.') + '</div>' +
      '<div class="flex"><button class="btn" id="ur-ar">🖨 ' + I18n.t('تصدير التقرير (عربي)') + '</button>' +
      '<button class="btn" id="ur-en">🖨 Export Report (English)</button></div></div>';

    // ملاحظة: عنوانا نافذتَي الطباعة أدناه ("تقرير التسليم الموحّد" / "Unified Handover Report") يبقيان ثابتَين
    // حسب لغة التصدير المختارة (الزر) بغض النظر عن لغة الواجهة الحالية، تماماً مثل كائن L في unifiedHtml أدناه.
    el.querySelector('#ur-ar').addEventListener('click', function () { printDoc('تقرير التسليم الموحّد', unifiedHtml(ctx, 'ar')); });
    el.querySelector('#ur-en').addEventListener('click', function () { printDoc('Unified Handover Report', unifiedHtml(ctx, 'en')); });
  }

  function unifiedHtml(ctx, lang) {
    const P = ctx.S.projects[0] || {};
    const items = ctx.S.handoverItems || [];
    const punch = ctx.S.punchList || [];
    const warr = ctx.S.warranties || [];
    const L = lang === 'en'
      ? { title: 'Unified Handover Report', project: 'Project', docs: 'Documents & Submittals', reg: 'Regulatory Requirements', item: 'Item', resp: 'Responsible', status: 'Status', punch: 'Punch List Summary', ref: 'Ref', contractor: 'Contractor', note: 'Note', state: 'State', warr: 'Warranties', wend: 'Expiry', open: 'Open', closed: 'Closed',
          st: { pending: 'Pending', in_progress: 'In Progress', done: 'Completed', na: 'N/A' } }
      : { title: 'تقرير التسليم الموحّد', project: 'المشروع', docs: 'المستندات والتسليمات', reg: 'المتطلبات النظامية', item: 'البند', resp: 'المسؤول', status: 'الحالة', punch: 'ملخص قائمة الملاحظات', ref: 'المرجع', contractor: 'المقاول', note: 'الملاحظة', state: 'الحالة', warr: 'الضمانات', wend: 'الانتهاء', open: 'مفتوحة', closed: 'مغلقة',
          st: { pending: 'لم يبدأ', in_progress: 'قيد الإنجاز', done: 'مكتمل', na: 'غير مطلوب' } };
    function grpTable(grp) {
      return '<table><thead><tr><th>' + L.item + '</th><th>' + L.resp + '</th><th>' + L.status + '</th></tr></thead><tbody>' +
        items.filter(function (i) { return i.group === grp; }).map(function (it) {
          const name = lang === 'en' ? (it.en || it.title) : it.title;
          return '<tr><td>' + esc(name) + '</td><td>' + esc(it.responsible || '—') + '</td><td class="' + (it.status === 'done' ? 'ok' : '') + '">' + esc(L.st[it.status] || it.status) + '</td></tr>';
        }).join('') + '</tbody></table>';
    }
    return '<h1>' + L.title + '</h1>' +
      '<p class="muted">' + L.project + ': <b>' + esc(P.name) + '</b>' + (P.location ? ' — ' + esc(P.location) : '') + '</p>' +
      '<h2>' + L.docs + '</h2>' + grpTable('docs') +
      '<h2>' + L.reg + '</h2>' + grpTable('regulatory') +
      '<h2>' + L.punch + '</h2>' +
      '<p>' + L.open + ': <b class="bad">' + punch.filter(function (x) { return x.status === 'open'; }).length + '</b> — ' + L.closed + ': <b class="ok">' + punch.filter(function (x) { return x.status === 'closed'; }).length + '</b></p>' +
      '<table><thead><tr><th>' + L.ref + '</th><th>' + L.contractor + '</th><th>' + L.note + '</th><th>' + L.state + '</th></tr></thead><tbody>' +
      punch.map(function (it) {
        return '<tr><td>' + esc(it.ref || '') + '</td><td>' + esc(contractorName(ctx, it.contractorId)) + '</td><td>' + esc(it.title) + '</td>' +
          '<td class="' + (it.status === 'closed' ? 'ok' : 'bad') + '">' + (it.status === 'closed' ? L.closed : L.open) + '</td></tr>';
      }).join('') + '</tbody></table>' +
      '<h2>' + L.warr + '</h2><table><thead><tr><th>' + L.item + '</th><th>' + L.contractor + '</th><th>' + L.wend + '</th></tr></thead><tbody>' +
      warr.map(function (w) { return '<tr><td>' + esc(w.item) + '</td><td>' + esc(contractorName(ctx, w.contractorId)) + '</td><td>' + esc(w.endDate || '') + '</td></tr>'; }).join('') + '</tbody></table>';
  }

  // ============ تكوين مراحل المشروع (قابل للتخصيص حسب النوع) ============
  function renderPhases(el, ctx) {
    const P = ctx.S.projects[0] || {};
    const tasks = (ctx.S.scheduleTasks || []).slice();
    const lib = ctx.S.phaseLibrary || [];
    const templates = ctx.S.phaseTemplates || {};
    const canEdit = ['consultant', 'admin'].indexOf(ctx.U.role) !== -1;

    el.innerHTML =
      '<div class="card mb"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
      '<h3 style="margin:0">🗂️ ' + I18n.t('مراحل المشروع') + ' <span class="hint">' + I18n.t('نوع المشروع: ') + esc(P.type || I18n.t('غير محدد')) + ' — ' + I18n.t('قابلة للتخصيص') + '</span></h3>' +
      (canEdit ? '<div class="flex"><select class="inp" id="ph-tmpl" style="max-width:220px"><option value="">' + I18n.t('تطبيق قالب حسب النوع...') + '</option>' +
        Object.keys(templates).map(function (t) { return '<option value="' + esc(t) + '">' + esc(t === 'default' ? I18n.t('قالب افتراضي') : t) + '</option>'; }).join('') + '</select>' +
        '<button class="btn sm" id="ph-add">➕ ' + I18n.t('مرحلة') + '</button></div>' : '') + '</div>' +
      '<div class="small muted">' + I18n.t('أضف أو احذف المراحل بما يناسب طبيعة المشروع — تنعكس في لوحة القيادة ومقارنات الإنجاز.') + '</div></div>' +

      (canEdit ? (function () {
        const th = VS.thresholds(P);
        return '<div class="card mb"><div class="flex" style="justify-content:space-between;flex-wrap:wrap">' +
          '<h3 style="margin:0">⚙️ ' + I18n.t('عتبات التنبيه لهذا المشروع') + ' <span class="hint">' + I18n.t('تضبط متى تُطلق التنبيهات وتُحتسب المؤشرات') + '</span></h3>' +
          '<button class="btn sm" id="th-save">💾 ' + I18n.t('حفظ العتبات') + '</button></div>' +
          '<div class="grid g4" style="margin-top:12px">' +
          '<div><label class="fl">' + I18n.t('مهلة المراجعة (SLA) — يوم') + '</label><input class="inp num" id="th-sla" type="number" min="1" value="' + th.slaReviewDays + '"></div>' +
          '<div><label class="fl">' + I18n.t('تنبيه قرب انتهاء الضمان — يوم') + '</label><input class="inp num" id="th-warr" type="number" min="1" value="' + th.warrantyWarnDays + '"></div>' +
          '<div><label class="fl">' + I18n.t('حد تأخر المقاول — %') + '</label><input class="inp num" id="th-delay" type="number" min="0" value="' + th.contractorDelayPct + '"></div>' +
          '<div><label class="fl">' + I18n.t('حد انحراف التقدّم (كهرماني/أحمر) — نقطة') + '</label><input class="inp num" id="th-dev" type="number" min="1" value="' + th.progressAmberPct + '"></div>' +
          '<div><label class="fl">' + I18n.t('درجة تنبيه الصحة (عند/دون)') + '</label><select class="inp" id="th-grade">' +
          ['A', 'B', 'C', 'D'].map(function (g) { return '<option value="' + g + '"' + (th.healthAlertGrade === g ? ' selected' : '') + '>' + g + (g === 'B' ? I18n.t(' — جيد فأدنى') : g === 'C' ? I18n.t(' — مقبول فأدنى') : g === 'D' ? I18n.t(' — حرج فقط') : I18n.t(' — أي هبوط')) + '</option>'; }).join('') + '</select></div>' +
          '</div>' +
          '<div class="small muted mt">' + I18n.t('تُطبَّق فوراً على تتبع التقديمات (SLA)، وتنبيهات الضمانات، ومقارنة المقاولين، وتنبيه هبوط/تحسّن صحة المشروع.') + '</div></div>';
      })() : '') +

      '<div class="card"><div class="tbl-wrap"><table class="tbl"><thead><tr><th>#</th><th>' + I18n.t('المرحلة') + '</th><th>' + I18n.t('مخطط') + '</th><th>' + I18n.t('فعلي') + '</th><th>' + I18n.t('الإنجاز') + '</th>' + (canEdit ? '<th></th>' : '') + '</tr></thead><tbody>' +
      tasks.map(function (t, i) {
        return '<tr><td class="num small">' + (i + 1) + '</td>' +
          '<td><b>' + esc(t.name) + '</b>' + (t.phaseKey ? '<div class="small muted" dir="ltr" style="text-align:right">' + esc((lib.find(function (l) { return l.key === t.phaseKey; }) || {}).en || '') + '</div>' : '') + '</td>' +
          '<td class="small muted num">' + esc(t.startPlanned || '') + ' ← ' + esc(t.endPlanned || '') + '</td>' +
          '<td class="small muted num">' + esc(t.startActual || '—') + ' ← ' + esc(t.endActual || '—') + '</td>' +
          '<td><div class="flex" style="gap:6px"><div class="bar" style="flex:1"><i style="width:' + (t.progress || 0) + '%"></i></div><b class="num small">' + (t.progress || 0) + '%</b></div></td>' +
          (canEdit ? '<td><div class="flex" style="gap:4px"><button class="btn ghost sm" data-phedit="' + t.id + '">' + I18n.t('تحديث') + '</button><button class="btn danger sm" data-phdel="' + t.id + '">' + I18n.t('حذف') + '</button></div></td>' : '') +
          '</tr>';
      }).join('') + '</tbody></table></div>' +
      (tasks.length ? '' : '<div class="empty"><div class="e-ico">🗂️</div>' + I18n.t('لا مراحل — طبّق قالباً أو أضف مرحلة') + '</div>') + '</div>';

    const thSave = el.querySelector('#th-save');
    if (thSave) thSave.addEventListener('click', async function () {
      try {
        await Api.update('projects', P.id, {
          thresholds: {
            slaReviewDays: Math.max(1, Number(el.querySelector('#th-sla').value) || 7),
            warrantyWarnDays: Math.max(1, Number(el.querySelector('#th-warr').value) || 90),
            contractorDelayPct: Math.max(0, Number(el.querySelector('#th-delay').value) || 3),
            progressAmberPct: Math.max(1, Number(el.querySelector('#th-dev').value) || 10),
            healthAlertGrade: el.querySelector('#th-grade').value || 'C'
          }
        });
        toast(I18n.t('✅ حُفظت عتبات التنبيه — طُبّقت على مؤشرات هذا المشروع')); ctx.refresh();
      } catch (e) { toast(e.message, true); }
    });

    const tmpl = el.querySelector('#ph-tmpl');
    if (tmpl) tmpl.addEventListener('change', async function () {
      const key = tmpl.value;
      if (!key || !templates[key]) return;
      if (!confirm(I18n.t('تطبيق قالب "') + (key === 'default' ? I18n.t('افتراضي') : key) + I18n.t('"؟ سيضيف المراحل غير الموجودة.'))) { tmpl.value = ''; return; }
      const existing = tasks.map(function (t) { return t.phaseKey; });
      try {
        for (const pk of templates[key]) {
          if (existing.indexOf(pk) !== -1) continue;
          const ph = lib.find(function (l) { return l.key === pk; });
          if (ph) await Api.create('scheduleTasks', { phaseKey: pk, name: ph.ar, progress: 0, startPlanned: '', endPlanned: '', startActual: null, endActual: null });
        }
        toast(I18n.t('✅ طُبّق القالب وأُضيفت المراحل')); ctx.refresh();
      } catch (e) { toast(e.message, true); }
    });

    const addBtn = el.querySelector('#ph-add');
    if (addBtn) addBtn.addEventListener('click', function () { phaseModal(ctx, null, lib); });
    el.querySelectorAll('[data-phedit]').forEach(function (b) {
      b.addEventListener('click', function () { phaseModal(ctx, tasks.find(function (t) { return t.id === b.getAttribute('data-phedit'); }), lib); });
    });
    el.querySelectorAll('[data-phdel]').forEach(function (b) {
      b.addEventListener('click', async function () {
        if (!confirm(I18n.t('حذف هذه المرحلة؟'))) return;
        try { await Api.remove('scheduleTasks', b.getAttribute('data-phdel')); toast(I18n.t('حُذفت المرحلة')); ctx.refresh(); }
        catch (e) { toast(e.message, true); }
      });
    });
  }

  function phaseModal(ctx, t, lib) {
    const isEdit = !!t;
    t = t || {};
    const m = modal(
      '<h3>' + (isEdit ? '✏️ ' + I18n.t('تحديث مرحلة') : '➕ ' + I18n.t('مرحلة جديدة')) + '</h3>' +
      '<label class="fl">' + I18n.t('من المكتبة (اختياري)') + '</label><select class="inp" id="ph-lib"><option value="">' + I18n.t('— مخصصة —') + '</option>' +
      lib.map(function (l) { return '<option value="' + l.key + '"' + (t.phaseKey === l.key ? ' selected' : '') + '>' + esc(l.ar) + '</option>'; }).join('') + '</select>' +
      '<label class="fl">' + I18n.t('اسم المرحلة') + '</label><input class="inp" id="ph-name" value="' + esc(t.name || '') + '">' +
      '<div class="grid g2"><div><label class="fl">' + I18n.t('بداية مخططة') + '</label><input class="inp" id="ph-sp" type="date" value="' + esc(t.startPlanned || '') + '"></div>' +
      '<div><label class="fl">' + I18n.t('نهاية مخططة') + '</label><input class="inp" id="ph-ep" type="date" value="' + esc(t.endPlanned || '') + '"></div></div>' +
      '<div class="grid g2"><div><label class="fl">' + I18n.t('بداية فعلية') + '</label><input class="inp" id="ph-sa" type="date" value="' + esc(t.startActual || '') + '"></div>' +
      '<div><label class="fl">' + I18n.t('نهاية فعلية') + '</label><input class="inp" id="ph-ea" type="date" value="' + esc(t.endActual || '') + '"></div></div>' +
      '<label class="fl">' + I18n.t('نسبة الإنجاز %') + '</label><input class="inp num" id="ph-prog" type="number" min="0" max="100" value="' + (t.progress || 0) + '">' +
      '<div class="m-actions"><button class="btn" id="ph-ok">' + I18n.t('حفظ') + '</button><button class="btn mutedb" id="ph-cancel">' + I18n.t('إلغاء') + '</button></div>'
    );
    const libSel = m.querySelector('#ph-lib');
    libSel.addEventListener('change', function () {
      const l = lib.find(function (x) { return x.key === libSel.value; });
      if (l) m.querySelector('#ph-name').value = l.ar;
    });
    m.querySelector('#ph-cancel').addEventListener('click', function () { m.remove(); });
    m.querySelector('#ph-ok').addEventListener('click', async function () {
      const name = m.querySelector('#ph-name').value.trim();
      if (!name) { toast(I18n.t('أدخل اسم المرحلة'), true); return; }
      const data = {
        phaseKey: libSel.value || null, name: name,
        startPlanned: m.querySelector('#ph-sp').value, endPlanned: m.querySelector('#ph-ep').value,
        startActual: m.querySelector('#ph-sa').value || null, endActual: m.querySelector('#ph-ea').value || null,
        progress: Number(m.querySelector('#ph-prog').value) || 0
      };
      try {
        if (isEdit) await Api.update('scheduleTasks', t.id, data);
        else await Api.create('scheduleTasks', data);
        m.remove(); toast(I18n.t('✅ حُفظت المرحلة')); ctx.refresh();
      } catch (e) { toast(e.message, true); }
    });
  }

  // ============ لوحة محفظة المشاريع (ممثل المالك / الأدمن) ============
  const GRADE_PILL = { A: 'p-ok', B: 'p-ok', C: 'p-warn', D: 'p-danger' };
  const HC = { ok: '#2dd4a0', warn: '#ffcc00', danger: '#ff3b30' };

  function renderPortfolio(el, ctx) {
    const A = ctx.Sall || ctx.S;
    const projects = A.projects || [];
    const ph = A.projectHealth || {};
    const hist = A.healthHistory || [];

    function trendArrow(pid, score) {
      const h = hist.filter(function (x) { return x.projectId === pid; }).sort(function (a, b) { return String(a.month).localeCompare(String(b.month)); });
      if (!h.length) return '';
      const d = score - h[h.length - 1].score;
      return d > 0 ? '<span style="color:var(--ok)">▲ +' + d + '</span>' : d < 0 ? '<span style="color:var(--danger)">▼ ' + d + '</span>' : '<span class="muted">—</span>';
    }

    const rows = projects.map(function (p) {
      const h = ph[p.id] || { score: 0, grade: '—', cls: 'warn', gradeAr: '', issues: {} };
      const g = projectGlanceLite(p);
      return { p: p, h: h, g: g };
    });
    const avg = rows.length ? Math.round(rows.reduce(function (a, r) { return a + r.h.score; }, 0) / rows.length) : 0;
    const atRisk = rows.filter(function (r) { return r.h.cls === 'warn' || r.h.cls === 'danger'; }).length;
    const totalBudget = rows.reduce(function (a, r) { return a + (r.p.budgetPlanned || 0); }, 0);
    const totalCost = rows.reduce(function (a, r) { return a + (r.p.costActual || 0); }, 0);
    const wProg = totalBudget ? Math.round(rows.reduce(function (a, r) { return a + (r.p.progressActual || 0) * (r.p.budgetPlanned || 0); }, 0) / totalBudget) : 0;

    el.innerHTML =
      '<div class="grid g4 mb">' +
      '<div class="card kpi"><div class="lbl">' + I18n.t('عدد المشاريع') + '</div><div class="val num">' + rows.length + '</div><div class="sub">' + I18n.t('تحت الإشراف') + '</div></div>' +
      '<div class="card kpi ' + (avg >= 70 ? 'k-ok' : avg >= 55 ? 'k-warn' : 'k-danger') + '"><div class="lbl">' + I18n.t('متوسط صحة المحفظة') + '</div><div class="val num">' + avg + '</div><div class="sub">' + I18n.t('من 100') + '</div></div>' +
      '<div class="card kpi ' + (atRisk ? 'k-warn' : 'k-ok') + '"><div class="lbl">' + I18n.t('مشاريع تحت المخاطرة') + '</div><div class="val num">' + atRisk + '</div><div class="sub">' + I18n.t('فئة C أو D') + '</div></div>' +
      '<div class="card kpi k-info"><div class="lbl">' + I18n.t('إجمالي الميزانيات') + '</div><div class="val" style="font-size:19px">' + VS.millions(totalBudget) + '</div><div class="sub num">' + I18n.t('الإنجاز المرجّح ') + wProg + '%</div></div>' +
      '</div>' +

      '<div class="card mb"><h3>🗂️ ' + I18n.t('محفظة المشاريع — مقارنة الصحة') + ' <span class="hint">' + I18n.t('اضغط "فتح" للعمل على أي مشروع') + '</span></h3>' +
      '<div class="tbl-wrap"><table class="tbl"><thead><tr>' +
      '<th>' + I18n.t('المشروع') + '</th><th>' + I18n.t('النوع') + '</th><th>' + I18n.t('الإنجاز (فعلي/مخطط)') + '</th><th>' + I18n.t('التكلفة') + '</th><th>' + I18n.t('مؤشر الصحة') + '</th><th>' + I18n.t('الاتجاه') + '</th><th>' + I18n.t('مخاطر مفتوحة') + '</th><th></th></tr></thead><tbody>' +
      rows.map(function (r) {
        const p = r.p, h = r.h;
        const risks = (h.issues.openNcr || 0) + (h.issues.failedTests || 0) + (h.issues.openPunch || 0) + (h.issues.openHse || 0) + (h.issues.openInc || 0);
        const active = ctx.projectId === p.id;
        return '<tr>' +
          '<td><b>🏗️ ' + esc(p.name) + '</b><div class="small muted">' + esc(p.location || '') + '</div></td>' +
          '<td class="small">' + esc(p.type || '—') + '</td>' +
          '<td><div class="flex" style="gap:6px"><div class="bar ' + (r.g.behind ? 'b-danger' : 'b-ok') + '" style="flex:1;min-width:90px"><i style="width:' + (p.progressActual || 0) + '%"></i></div>' +
          '<b class="num small">' + (p.progressActual || 0) + '%</b></div><div class="small muted num">' + I18n.t('مخطط ') + (p.progressPlanned || 0) + '%</div></td>' +
          '<td class="small num">' + VS.millions(p.costActual || 0) + '<div class="muted">' + I18n.t('من ') + VS.millions(p.budgetPlanned || 0) + '</div></td>' +
          '<td><div class="flex" style="gap:8px;align-items:center"><b class="num" style="font-size:17px;color:' + (HC[h.cls] || HC.warn) + '">' + h.score + '</b>' +
          '<span class="pill ' + (GRADE_PILL[h.grade] || 'p-muted') + '">' + h.grade + '</span></div></td>' +
          '<td class="small">' + trendArrow(p.id, h.score) + '</td>' +
          '<td>' + (risks ? '<span class="pill p-warn">' + risks + '</span>' : '<span class="pill p-ok">0</span>') + '</td>' +
          '<td>' + (active ? '<span class="pill p-ok">' + I18n.t('الحالي') + '</span>' : '<button class="btn sm" data-openp="' + p.id + '">' + I18n.t('فتح ←') + '</button>') + '</td>' +
          '</tr>';
      }).join('') + '</tbody></table></div>' +
      '<div class="flex mt"><button class="btn ghost sm" id="pf-print">🖨 ' + I18n.t('تصدير تقرير المحفظة PDF') + '</button></div></div>';

    el.querySelectorAll('[data-openp]').forEach(function (b) {
      b.addEventListener('click', function () {
        ctx.setProject(b.getAttribute('data-openp'));
        ctx.nav('dashboard');
      });
    });
    el.querySelector('#pf-print').addEventListener('click', function () {
      printDoc(I18n.t('تقرير محفظة المشاريع'), portfolioHtml(rows, { avg: avg, atRisk: atRisk, totalBudget: totalBudget, wProg: wProg }));
    });
  }

  function projectGlanceLite(p) {
    const progVar = (p.progressActual || 0) - (p.progressPlanned || 0);
    return { behind: progVar < -3, progVar: Math.round(progVar * 10) / 10 };
  }

  function portfolioHtml(rows, agg) {
    return '<h1>' + I18n.t('تقرير محفظة المشاريع') + '</h1>' +
      '<p class="muted">' + I18n.t('عدد المشاريع: ') + '<b>' + rows.length + '</b> ' + I18n.t('· متوسط الصحة: ') + '<b>' + agg.avg + '/100</b> ' + I18n.t('· تحت المخاطرة: ') + '<b>' + agg.atRisk + '</b> ' + I18n.t('· إجمالي الميزانيات: ') + '<b>' + (agg.totalBudget / 1e6).toFixed(1) + 'M</b> ' + I18n.t('· الإنجاز المرجّح: ') + '<b>' + agg.wProg + '%</b></p>' +
      '<table><thead><tr><th>' + I18n.t('المشروع') + '</th><th>' + I18n.t('النوع') + '</th><th>' + I18n.t('الإنجاز الفعلي') + '</th><th>' + I18n.t('المخطط') + '</th><th>' + I18n.t('مؤشر الصحة') + '</th><th>' + I18n.t('الفئة') + '</th></tr></thead><tbody>' +
      rows.map(function (r) {
        return '<tr><td>' + esc(r.p.name) + '</td><td>' + esc(r.p.type || '') + '</td><td>' + (r.p.progressActual || 0) + '%</td><td>' + (r.p.progressPlanned || 0) + '%</td>' +
          '<td>' + r.h.score + '</td><td class="' + (r.h.cls === 'ok' ? 'ok' : 'bad') + '">' + r.h.grade + ' — ' + esc(r.h.gradeAr) + '</td></tr>';
      }).join('') + '</tbody></table>';
  }

  window.ViewsHandover = {
    renderHandover: renderHandover,
    renderPhases: renderPhases,
    renderPortfolio: renderPortfolio,
    printDoc: printDoc
  };
})();
